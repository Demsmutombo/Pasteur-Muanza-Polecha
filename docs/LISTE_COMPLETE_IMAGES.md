# Liste Complète des Images - Toutes les Images à Afficher

D'après les commandes exécutées, voici le nombre total d'images dans chaque dossier :

- **Cimetière** : 93 images
- **Morgue** : 77 images  
- **Image du Pasteur** : 10 images
- **VEILLER** : 86 images (non utilisées dans les galeries mais disponibles pour services)

## Action Requise

Pour afficher TOUTES les images, il faut :
1. Mettre à jour le tableau `cimetiereImages` avec les 93 images
2. Mettre à jour le tableau `morgueImages` avec les 77 images
3. Mettre à jour le tableau `pasteurPhotos` avec les 11 images (déjà fait)

**Note**: Vu le grand nombre d'images, cela peut ralentir le chargement de la page. Il serait recommandé de :
- Utiliser le lazy loading
- Ou sélectionner un nombre raisonnable d'images (ex: 30-50 pour cimetière, 30-40 pour morgue)
